(ns desafio.core
  (:gen-class))

(defn -main
  "I don't do a whole lot ... yet."
  [& args]
  (println "Hello, World!"))
(def answer ":(")

(def hai "O HAI!")

(defn square [x]
  ":(")

(defn average [a b]
  ":(")
